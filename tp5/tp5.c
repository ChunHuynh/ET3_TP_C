#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "functions.h"

int main()
{
	/*
	char *nom = read_name();
	lower_case(nom);
	printf("%s\n", nom);
	upper_case(nom);
	printf("%s\n", nom);
	
	delete_str( nom );
	
	int num = letter_to_name('.');
	printf("%d\n", num);
	
	
	char **tab = (char**) malloc(0*sizeof(char*)); 
	
	append(nom,tab,0);
	printf("%s\n",tab[0]);
	*/
	
	
	return 0;
}
