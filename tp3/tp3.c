#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "functions.h"

int main()
{
	/*printf("\n******** Ceasar's code ********\n");

	char*	word = malloc(100*sizeof(char));
	char* coded_word = malloc(sizeof(word)+1);
	

	get_string(word);
	printf("\nThe original message    : %s", word);

	coded_word = add_string(word, 1);

	printf("Coded with ceasar's code: %s\n", coded_word);


	string_delete(coded_word);
	string_delete(word);
	
	printf("\n******** Sissa's chess ********\n");
	long long int rice = rice_seed(8);
	printf("%lli\n", rice);
	*/

	



	return 0;
}